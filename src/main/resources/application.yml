
# ?? ?? ??
server:
  port: 8083

# database ?? ??
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jpaboard?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: sol
    password: aaaaaaaa
  thymeleaf:
    cache: false

  # spring data jpa ??
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: updatep
